import { Level } from '../src/bases/level';
// (global as any).g = require("@akashic/akashic-engine");

const g = require("@akashic/akashic-engine");

describe('Levelのテスト', () => {
  it('Level のインスタンスを生成する', () => {
    
  //   // const gameSpy = jest.spyOn(g.Game, 'width', 'get').mockReturnValue(320);

  //   g.game.width = 320;
  //   expect(g.game.width).toEqual(320);
  });
});